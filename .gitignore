# 이 저장소는 그대로 배포됩니다 — git push 하면 GitHub Pages 가 파일을 있는 그대로
# 올립니다. 그래서 "커밋하지 않는다" 와 "배포하지 않는다" 가 같은 뜻입니다.
#
# 작업 전에 뜨는 백업 폴더. 원본 그대로의 admin.js 와 admin.html 사본이 들어 있어
# 커밋하면 옛 화면이 사이트에 같이 올라가고, 사람이 어느 것이 진짜인지 헷갈립니다.
# 폴더와 그 안의 내용은 디스크에 그대로 둡니다 — 추적만 하지 않습니다.
.backup_claude_*/

# 에디터·OS 부스러기
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
