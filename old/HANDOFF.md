# onecue — 이어받는 분께

광고주가 **제품과 설명만** 넣으면 전략·컨셉 5안·콘티·영상까지 만드는 파이프라인입니다.
지금은 **뼈대**입니다. 기능과 데이터 흐름은 돌아가고, **디자인은 갈아끼우라고 비워뒀습니다.**

- 사이트 · https://onecue26.github.io
- 빌드 도구 없음. 파일을 고치고 `git push` 하면 1~2분 뒤 반영됩니다

---

## 화면 세 개

| 파일 | 누가 보나 | 하는 일 |
|---|---|---|
| `index.html` + `app.js` | 내부 | 진행 중인 건 목록 |
| `new.html` + `new.js` | **광고주** | 의뢰 접수. 이 사이트의 입구 |
| `project.html` + `project.js` | **광고주** | 진행 상황 · 5안 선택 · 콘티 승인 |

`config.js` 는 접속 설정입니다. **여기 있는 키는 공개용**이라 브라우저에 노출돼도 됩니다.
보호는 키를 숨겨서가 아니라 DB 쪽 정책(RLS)이 합니다.

---

## 디자인을 바꿀 때 — 여기만 보시면 됩니다

**색과 톤은 `style.css` 맨 위 `:root` 한 곳에 모여 있습니다.**

```css
--ground   페이지 바탕      --ink     본문 글자
--panel    카드 바탕        --muted   보조 글자
--sunk     눌린 면          --faint   더 옅은 글자
--rule     선              --cue     강조 (방송 큐 램프의 주홍)
--live     완료(초록)       --hold    사람이 판단할 자리(호박)
```

- 다크 모드는 바로 아래 두 블록에서 **같은 이름을 다시 정의**합니다. 색을 새로 만들지 말고 **이름을 재정의**해 주세요
- 폰트는 각 HTML 의 `<link>` 한 줄(Gothic A1 · JetBrains Mono)
- `new.html` · `project.html` 안에 `<style>` 이 조금 남아 있습니다. 그 화면에서만 쓰는 것들이라 그렇습니다

### 마크업을 바꿔도 되는 것 / 안 되는 것

| | |
|---|---|
| **자유롭게** | 클래스 이름, 배치, 태그 구조, 문구 |
| **⚠️ 건드리면 깨짐** | `id` 값들 — JS 가 이걸로 찾습니다<br>`data-pick` · `data-aspects` 속성<br>`<input>` 의 `value`(규격·초수 값이 그대로 DB 에 들어감) |

JS 안의 `el("...")` 로 찾는 id 목록을 먼저 훑어보시면 안전합니다.

---

## 데이터가 어떻게 흐르나

```
new.html 제출
   → clients (회사)  →  projects (건)  →  briefs (의뢰 내용)
                                       →  contacts (연락처)
                                       →  jobs (작업 큐)   ← 여기 쌓이면 사람이 만든다
                                              ↓
                    strategies · concepts · developments · cuts · assets
                                              ↓
project.html 에서 광고주가 5안 선택 / 콘티 승인  →  approvals
```

- **표는 13개.** 스키마 원본은 작업 저장소의 `schema.sql`
- **상태는 `projects.step` 한 곳**에만 있습니다. 화면·큐·문서가 전부 이걸 봅니다
- 단계는 `brief → facts → strategy → concepts → develop → storyboard → anchors → video → deliver`
- 그중 **사람이 반드시 개입하는 세 곳** — `strategy` · `concepts` · `storyboard`

---

## 지금 상태에서 알아두실 것

- **로그인이 없습니다.** 주소를 아는 사람은 데이터를 바꾸고 지울 수 있습니다.
  둘이 테스트하는 단계라 일부러 열어뒀습니다. 정식 공개 전에 반드시 조입니다
- **`contacts` 만 잠겨 있습니다** — 넣을 수는 있고 공개 키로는 못 읽습니다 (개인정보라서)
- `robots.txt` + `noindex` 로 **검색에는 안 잡힙니다**
- 데이터는 백업해 두었고 되돌릴 수 있습니다 (`tools/backup.py`)

---

## 아직 안 만든 것

- 로그인 · 광고주별 격리
- 제품 사진 업로드 (지금은 텍스트만)
- 진행 상황 자동 갱신 (지금은 새로고침해야 보입니다)
- 콘티를 광고주에게 보내는 **공유 링크**(토큰 방식)
- 영상 재생 화면
